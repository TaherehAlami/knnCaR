% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{distance}
\alias{distance}
\title{Calculate distances between train data and new data for knn_s3 model}
\usage{
distance(object, newdata, distmethod = c("sse", "sad"), ...)
}
\arguments{
\item{object}{An object of \code{knn_s3} \code{\link{class}}.}

\item{newdata}{A numeric matrix or data frame of new observations.}

\item{distmethod}{Distance metric to use: \code{"sse"} for sum of squared errors (default) or
\code{"sad"} for sum of absolute differences.}

\item{...}{Additional parameters.}
}
\value{
A numeric matrix of distances with \code{nrow(newdata)} rows and \code{nrow(object$train_x)} columns.
}
\description{
This function computes pairwise distances between the training data stored in a \code{knn_s3} object
and new data points, using either sum of squared errors ("sse") or sum of absolute differences ("sad").
It supports matrix or data frame inputs for the new data.
}
\examples{
\dontrun{
fit <- knn_s3(Species ~ Sepal.Length + Sepal.Width, data = iris, k = 5)
new_points <- iris[1:3, 1:2]
distances <- distance(fit, new_points, distmethod = "sse")
print(distances)
}

}
